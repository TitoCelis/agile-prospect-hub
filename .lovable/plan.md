

## Reemplazo de Favicon

Se reemplazará el favicon actual por el nuevo logo de CMI proporcionado.

### Pasos:

1. **Copiar el archivo** `user-uploads://LOGO_CMI_FINAL_PPFno_letter-2.png` a `public/favicon.png`

2. **Actualizar `index.html`**: Cambiar la referencia del favicon de `/favicon.ico` a `/favicon.png` con el tipo MIME correcto (`image/png`)

